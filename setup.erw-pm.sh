#
# EM April 14 (update)
#
# Remark: special case because this is the project which contains the erw-pm system.
#
#echo "debug: in erw-bash-commons setup" 1>&2
addToEnvVar "$PWD/lib" PATH ":"
addToEnvVar "$PWD/bin" PATH ":"
